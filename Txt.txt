<!DOCTYPE html>
<html lang="kk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modern CS Dashboard</title>
    <style>
        :root {
            --bg: #0f172a;
            --card-bg: rgba(30, 41, 59, 0.7);
            --accent: #38bdf8;
            --text: #f1f5f9;
        }

        body {
            background-color: var(--bg);
            color: var(--text);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 40px;
            margin: 0;
        }

        /* Modern Marquee Replacement */
        .scrolling-header {
            width: 100%;
            overflow: hidden;
            background: linear-gradient(90deg, transparent, var(--accent), transparent);
            padding: 10px 0;
            margin-bottom: 30px;
            white-space: nowrap;
        }

        .scrolling-text {
            display: inline-block;
            font-weight: bold;
            letter-spacing: 2px;
            animation: scroll-left 15s linear infinite;
        }

        @keyframes scroll-left {
            0% { transform: translateX(100%); }
            100% { transform: translateX(-100%); }
        }

        /* Beautiful Table Styling */
        .data-container {
            background: var(--card-bg);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 800px;
        }

        h2 {
            color: var(--accent);
            border-bottom: 2px solid var(--accent);
            padding-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        th {
            background-color: rgba(56, 189, 248, 0.1);
            color: var(--accent);
            text-transform: uppercase;
            font-size: 0.8rem;
        }

        tr:hover {
            background-color: rgba(255, 255, 255, 0.05);
            transition: 0.3s;
        }

        .cs-concept {
            margin-top: 30px;
            font-style: italic;
            color: #94a3b8;
        }
    </style>
</head>
<body>

    <div class="scrolling-header">
        <div class="scrolling-text">
            COMPUTER SCIENCE: АЛГОРИТМДЕР ЖӘНЕ ДЕРЕКТЕР ҚҰРЫЛЫМЫ — ШЫНДЫҚ ОСЫНДА! 
        </div>
    </div>

    <div class="data-container">
        <h2>Алгоритм Күрделілігі (Complexity)</h2>
        <table>
            <thead>
                <tr>
                    <th>Белгіленуі (Notation)</th>
                    <th>Атауы (Name)</th>
                    <th>Тиімділігі (Efficiency)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>O(1)</td>
                    <td>Тұрақты (Constant)</td>
                    <td style="color: #4ade80;">Керемет (W)</td>
                </tr>
                <tr>
                    <td>O(n)</td>
                    <td>Сызықтық (Linear)</td>
                    <td style="color: #facc15;">Орташа</td>
                </tr>
                <tr>
                    <td>O(n²)</td>
                    <td>Квадраттық (Quadratic)</td>
                    <td style="color: #f87171;">Нашар (L)</td>
                </tr>
            </tbody>
        </table>

        <div class="cs-concept">
            <p><strong>Science Fact:</strong> Алгоритмнің тиімділігін білу — бұл жай ғана код жазу емес, бұл ресурстарды үнемдеу туралы ғылым. Нағыз программист әрқашан O(n²) - тан қашуға тырысады. 💔</p>
        </div>
    </div>

</body>
</html>
